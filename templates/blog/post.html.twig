{% extends 'base.html.twig' %}

{% block body %}
    <h1>Post</h1>
    <ul>
        <li>
            <h2>{{ post.title }}</h2>
            <p>{{ post.content }}</p>
            <p>Created at: {{ post.createdAt|date('Y-m-d H:i:s') }}</p>
        </li>
    </ul>
{% endblock %}